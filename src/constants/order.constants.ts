export enum Status {
    New = "new",
    Canceled = "canceled",
    Submitted = "submitted",
    Opened = "opened",
    Completed = "completed",
    Failed = "failed",
}

export enum Group {
    Entry = "entry",
    EntryCancel = "entry_cancel",
    Exit = "exit",
    ExitCancel = "exit_cancel",
    Extra = "extra",
    ScaleIn = "scale_in",
    ScaleOut = "scale_out",
}